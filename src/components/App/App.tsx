import { Routes } from "react-router-dom";
import Header from "../Header/Header";

function App() {
  return (
    <main className="container mx-auto">
        <Header/>
        <Routes>
            {/* <Route path="*" element={}/> */}
        </Routes>
    </main>
  );
}

export default App;
